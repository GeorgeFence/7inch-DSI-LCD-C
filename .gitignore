# CodeQL analysis artifact
_codeql_detected_source_root

# Kernel module build artifacts
*.ko
*.o
*.mod
*.mod.c
*.order
*.symvers
.tmp_versions/
modules.builtin
modules.builtin.modinfo

# Device tree compiled binaries (generated by `make dtbs`)
# Only .dts source files are tracked; .dtbo are build outputs
64/6.12.47/pi4/Driver_package/*.dtbo
